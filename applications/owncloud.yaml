apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: owncloud
  namespace: argo-cd
spec:
  destination:
    namespace: owncloud
    server: https://kubernetes.default.svc
  project: default
  source:
    path: namespaces/owncloud
    repoURL: git@github.com:malorrx/edmee-kube.git
    targetRevision: main
  syncPolicy:
    automated:
      enabled: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
